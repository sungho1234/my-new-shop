# SMS 인증 설정 가이드 (NHN Cloud)

이 가이드는 NHN Cloud SMS 서비스를 설정하고 환경변수를 등록하는 방법을 설명합니다.

---

## 1. NHN Cloud 회원가입 및 프로젝트 생성

### 1-1. NHN Cloud 회원가입
1. [NHN Cloud 콘솔](https://console.nhncloud.com/)에 접속
2. 회원가입 (카카오, 네이버, 페이코, NHN 계정으로 가능)
3. 로그인

### 1-2. 프로젝트 생성
1. 콘솔 상단의 **프로젝트 생성** 클릭
2. 프로젝트 이름 입력 (예: `my-shop-sms`)
3. **확인** 버튼 클릭

---

## 2. SMS 서비스 활성화

### 2-1. SMS 서비스 활성화
1. 생성한 프로젝트 선택
2. 좌측 메뉴에서 **Notification > SMS** 선택
3. **서비스 활성화** 버튼 클릭

### 2-2. 발신번호 등록
SMS 발송을 위해서는 **발신번호 인증**이 필요합니다.

#### 방법 1: 개인 인증 (개인 휴대폰 번호)
1. SMS 콘솔 > **발신번호 관리** 탭
2. **발신번호 등록** 클릭
3. 본인 휴대폰 번호 입력 (예: `010-1234-5678`)
4. SMS로 받은 인증번호 입력
5. 등록 완료 (즉시 사용 가능)

#### 방법 2: 사업자 인증 (대표번호 사용)
1. SMS 콘솔 > **발신번호 관리** 탭
2. **발신번호 등록** 클릭
3. 사업자 등록번호 입력
4. 사업자등록증 업로드
5. 심사 완료 후 사용 가능 (1~3일 소요)

> ⚠️ **주의**: 개발/테스트 단계에서는 **개인 인증**으로 충분합니다. 실제 서비스 운영 시 사업자 인증을 권장합니다.

---

## 3. API 키 발급

### 3-1. AppKey 확인
1. SMS 콘솔 상단의 **URL & Appkey** 클릭
2. **Appkey** 복사 (예: `abc123def456...`)

### 3-2. SecretKey 생성
1. 우측 상단 프로필 아이콘 클릭
2. **API 보안 설정** 선택
3. **SecretKey 만들기** 클릭
4. SecretKey 생성 완료 → **복사** (한 번만 표시되므로 안전한 곳에 보관)

---

## 4. 환경변수 설정

프로젝트 루트의 `.env.local` 파일에 다음 환경변수를 추가하세요.

```env
# NHN Cloud SMS 설정
NHN_SMS_APP_KEY=여기에_AppKey_붙여넣기
NHN_SMS_SECRET_KEY=여기에_SecretKey_붙여넣기
NHN_SMS_SENDER_NUMBER=01012345678  # 하이픈 없이 입력
```

### 예시:
```env
NHN_SMS_APP_KEY=abc123def456ghi789
NHN_SMS_SECRET_KEY=xyz987wvu654tsr321
NHN_SMS_SENDER_NUMBER=01012345678
```

---

## 5. 개발 모드 (환경변수 없이 테스트)

환경변수가 설정되지 않은 경우, **개발 모드**로 동작합니다.

### 개발 모드 특징:
- 실제 SMS를 발송하지 않음
- 인증 코드가 **콘솔에 출력**됨
- 프론트엔드에서도 `alert`로 코드 표시 (개발 중에만)

### 콘솔 출력 예시:
```
─────────────────────────────────────
📱 [개발 모드] SMS 인증 코드
전화번호: 010-1234-5678
인증코드: 123456
─────────────────────────────────────
```

이 모드는 **환경변수 없이 SMS 인증 플로우를 테스트**할 수 있도록 설계되었습니다.

---

## 6. 비용 안내

### NHN Cloud SMS 요금:
| 구분 | 가격 |
|------|------|
| **가입비/월 사용료** | 무료 |
| **SMS 발송** | 8원/건 |
| **LMS 발송** | 20원/건 |
| **MMS 발송** | 30원/건 |

### 충전 방법:
1. 콘솔 우측 상단 **충전** 클릭
2. 원하는 금액 선택 (최소 10,000원)
3. 카드 결제 또는 계좌이체

> 💡 **Tip**: 개발/테스트 단계에서는 10,000원 충전으로 충분합니다 (약 1,250건 발송 가능).

---

## 7. 테스트

### 7-1. 서버 실행
```bash
npm run dev
```

### 7-2. 회원가입 플로우 테스트
1. 네이버 또는 구글로 로그인
2. `/signup/complete` 페이지로 자동 리다이렉트
3. 전화번호 입력 (예: `010-1234-5678`)
4. **인증요청** 버튼 클릭
5. SMS 수신 확인 (개발 모드면 콘솔 확인)
6. 인증 코드 입력 후 **확인** 버튼
7. 생년월일, 성별 입력 (선택)
8. **가입 완료하기** 클릭

### 7-3. 예상 결과
- ✅ 인증 코드 SMS 수신 (또는 콘솔 출력)
- ✅ 인증 성공 메시지
- ✅ 프로필 업데이트 완료
- ✅ 메인 페이지로 리다이렉트

---

## 8. 문제 해결

### Q1. SMS가 발송되지 않아요
- **원인 1**: 발신번호가 등록되지 않았거나 승인 대기 중
  - **해결**: SMS 콘솔 > 발신번호 관리에서 상태 확인
- **원인 2**: 잔액 부족
  - **해결**: 콘솔에서 충전
- **원인 3**: 환경변수 오류
  - **해결**: `.env.local` 파일 확인, 서버 재시작

### Q2. 개발 모드로 테스트하고 싶어요
- **해결**: `.env.local`에서 NHN 관련 환경변수를 주석 처리하거나 삭제
  ```env
  # NHN_SMS_APP_KEY=abc123...
  # NHN_SMS_SECRET_KEY=xyz987...
  # NHN_SMS_SENDER_NUMBER=01012345678
  ```

### Q3. 인증 코드가 만료되었다고 나와요
- **원인**: 인증 코드는 **5분** 유효
- **해결**: 다시 **인증요청** 버튼을 눌러 새 코드 받기

### Q4. "인증 시도 횟수를 초과했습니다" 오류
- **원인**: 같은 코드로 **5번** 이상 실패
- **해결**: 다시 **인증요청** 버튼을 눌러 새 코드 받기

---

## 9. 프로덕션 배포 시 주의사항

### 9-1. 환경변수 등록
Vercel 또는 다른 호스팅 서비스에 환경변수를 등록하세요.

**Vercel 예시**:
1. Vercel 대시보드 > 프로젝트 선택
2. **Settings** > **Environment Variables**
3. 다음 3개 변수 추가:
   - `NHN_SMS_APP_KEY`
   - `NHN_SMS_SECRET_KEY`
   - `NHN_SMS_SENDER_NUMBER`
4. **Save** 클릭 후 재배포

### 9-2. 발신번호 사업자 인증 권장
실제 서비스 운영 시에는 **사업자 인증된 대표번호**를 사용하는 것이 좋습니다.
- 개인 번호는 일일 발송 제한이 있을 수 있음
- 사업자 번호가 더 신뢰성 있게 보임

### 9-3. Redis 사용 고려
현재는 메모리 기반으로 인증 코드를 저장합니다.
프로덕션 환경에서는 **Redis**를 사용하는 것을 권장합니다.

```typescript
// lib/smsStore.ts를 Redis로 교체
import Redis from 'ioredis';
const redis = new Redis(process.env.REDIS_URL);

export const smsStore = {
  async set(phone: string, code: string) {
    await redis.set(`sms:${phone}`, code, 'EX', 300); // 5분
  },
  async verify(phone: string, code: string) {
    const saved = await redis.get(`sms:${phone}`);
    return saved === code;
  },
};
```

---

## 10. 요약

1. ✅ NHN Cloud 가입 및 프로젝트 생성
2. ✅ SMS 서비스 활성화
3. ✅ 발신번호 등록 (개인 인증으로 즉시 가능)
4. ✅ AppKey, SecretKey 발급
5. ✅ `.env.local`에 환경변수 추가
6. ✅ 서버 재시작 후 테스트
7. ✅ 개발 모드로 테스트 가능 (환경변수 없이도 동작)

---

## 관련 링크

- [NHN Cloud 콘솔](https://console.nhncloud.com/)
- [NHN Cloud SMS 문서](https://docs.nhncloud.com/ko/Notification/SMS/ko/overview/)
- [NHN Cloud API 가이드](https://docs.nhncloud.com/ko/Notification/SMS/ko/api-guide/)

---

**문제가 발생하면 콘솔 로그를 확인하고, 필요 시 NHN Cloud 고객센터에 문의하세요!**
